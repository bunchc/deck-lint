# Deployment Rules

This directory contains spectral linting rules related to the deployment of Kong Gateway and its components. The rules are designed to enforce best practices for deployment topologies, containerization, and infrastructure configuration.
